# 인증 구현 설명서 — 플랜두씨 다이어리 2 (ALEPH T07)

- 결과물: <https://dochihs.github.io/pds-diary/> (첫 화면 = 로그인 화면)
- 이어 붙인 T06 최종 제출: 결과물 <https://dochihs.github.io/pds-diary/#plan=1> · 소스 commit `c55e544c1bfd8b0485017e11dafa9d7fc9a10eec`
- 확인 기록 원본: `docs/evidence/` (실제 서버에서 `checks/auth-evidence.html`로 만든 기록, 비밀번호·토큰은 가림)

---

## ① 무엇으로 붙였나

**인증 서비스: Supabase Auth** (GoTrue **v2.197.0** — `/auth/v1/health`가 돌려준 버전), 이메일·비밀번호 방식.

- 비밀번호 보관: Supabase Auth가 **bcrypt**(비용 10)로 되돌릴 수 없게 바꿔 `auth.users.encrypted_password`에 보관. 이 앱의 코드·DB 표는 비밀번호를 받거나 저장하지 않습니다.
- 로그인 뒤 사람을 알아보는 값: **토큰** — 액세스 토큰(JWT, **ES256** 서명, 발급 뒤 **3600초** 만료) + 리프레시 토큰. 토큰 안의 `session_id`로 서버의 세션 행(`auth.sessions`)을 가리킵니다.
- 브라우저 쪽은 라이브러리 없이 Supabase Auth REST API(`/auth/v1/signup`, `/token`, `/logout`, `/user`)를 `fetch`로 부릅니다.
- 자료 접근은 T06처럼 DB 함수(RPC)로만 하고, 함수가 토큰의 사용자와 세션을 확인합니다.

## ② 왜 그걸 골랐나

- T06이 이미 Supabase(DB + Data API) 위에 있어서, 같은 프로젝트 안의 Supabase Auth를 쓰면 **DB 함수가 토큰을 바로 믿고 확인**할 수 있습니다(서버를 하나 더 두지 않음). 토큰 서명 비밀키도 Supabase 안에만 있고 저는 만질 일이 없습니다.
- 비밀번호 해시·소금값·토큰 서명·만료는 직접 만들면 실수하기 쉬운 부분이라, 널리 쓰이는 서비스에 맡겼습니다(카드 2 C107).

**함께 검토했지만 고르지 않은 방법**

| 방법 | 고르지 않은 이유 |
|---|---|
| 직접 구현 (DB 안에 pgcrypto bcrypt + 세션 표) | 비밀번호·세션 만료·폐기를 모두 내가 책임져야 하고, 한 군데만 틀려도 전부 뚫립니다. 설명할 거리는 많지만 위험이 더 큽니다. |
| supabase-js 라이브러리 | 토큰 저장·갱신을 대신 해 주지만, T06이 외부 스크립트 없이 CSP로 같은 출처 파일만 실행하게 해 둔 것을 깨야 합니다. 필요한 호출은 REST 5개뿐이라 직접 불렀습니다. |
| Google 로그인(OAuth) | 비밀번호를 다루지 않아도 되지만, 로그인 결과가 주소(URL)에 실려 돌아오는 흐름이 생기고, 심사·시험 계정을 만들기 어렵습니다. |

## ③ 어디를 어떻게 고쳤나

네 흐름이 소스의 어디를 지나는지입니다. (소스 commit: 제출 때 고정 주소로 적음)

| 흐름 | 화면 `app.js` | 서버 |
|---|---|---|
| **가입** | `renderAuth()`의 가입 폼 → `signUp()` → `POST /auth/v1/signup` → `saveSession()`(탭의 sessionStorage) → `enterApp()` | Supabase Auth가 bcrypt로 저장하고 세션·토큰 발급. 같은 이메일은 422 |
| **로그인** | 로그인 폼 → `signIn()` → `POST /auth/v1/token?grant_type=password` | 틀린 비밀번호·없는 계정 모두 400 `invalid_credentials` → 화면 문구도 하나("이메일 또는 비밀번호가 맞지 않습니다.") |
| **로그아웃** | `doLogout()` → `signOut()` → `POST /auth/v1/logout?scope=local`(모든 기기: `global`) → 탭 토큰 삭제 | Supabase Auth가 `auth.sessions` 행을 지움 → 그 토큰은 `require_user()`에서 401 |
| **자료 조회** | `rpc()` — `Authorization: Bearer <토큰>`, 만료 60초 전 `refreshSession()`, 401이면 `endSession()`으로 로그인 화면 | `supabase/schema.sql`의 모든 공개 함수 맨 앞 `pds_private.require_user()` → `where id = … and owner_id = <토큰 사용자>` → 없으면 `pds_private.not_found()` = **404** |

그 밖에 T06에서 바꾼 곳

- `supabase/schema.sql`
  - `pds_private.require_user()`: ① 토큰의 `role = authenticated`와 `sub`(사용자) ② `session_id`가 `auth.sessions`에 살아 있는지. 둘 중 하나라도 아니면 401(`PDS401`). 서명·만료(exp)는 그 전에 Data API가 확인합니다.
  - 모든 표에 `owner_id` + `auth.users` 참조, 부모·자식을 `(id, owner_id)` 짝으로 묶는 `*_same_owner_fk` — 함수에 실수가 있어도 "남의 계획에 내 할 일" 같은 행은 DB가 만들지 않습니다.
  - 권한: 모든 함수의 실행 권한을 걷고 로그인한 역할에만 다시 줌. 로그인 안 한 역할(anon)은 `ping()`만(나머지 401). 표 직접 접근은 T06처럼 없음.
  - 표 사이 연결을 `RESTRICT → CASCADE`(계정 삭제 때 한꺼번에 지우려고. 화면·API에는 계획을 지우는 기능이 없음).
  - `delete_my_account()`: 확인 문구 "계정 삭제"를 받아 내 자료와 `auth.users`의 계정을 지움.
  - `pds_private.claim_t06_rows()`: 주인이 비어 있던 T06 행을 가입한 내 계정으로 옮기고 주인 칸을 NOT NULL로 잠금(값은 그대로).
- `index.html`: 공개 안내 → "내 기록은 로그인한 뒤에만" 안내, 로그인 전에는 계획·내보내기·메뉴를 숨김.

## ④ 안 열리는 것을 확인한 기록

실제 서버에서 시험 계정 A·B(같은 비밀번호)·C를 만들어 확인했습니다. 각 항목은 **성공한 요청**과 **거절된 요청**을 나란히 둡니다. 전체 요청·응답 원문은 `docs/evidence/`에 있습니다.

- 실행: 2026-09-24 17:47:10 ~ 17:47:17 (KST) · `checks/auth-evidence.html` · 요청 65건 · **16/16 통과**
- 원본: [`docs/evidence/T07_확인기록_20260924-k3gy.md`](evidence/T07_확인기록_20260924-k3gy.md) (아래 `#번호`는 그 기록의 요청 번호)
- 시험 계정: A·B(같은 비밀번호) = 서로 남의 자료 시험용, C = 비밀번호 변경·계정 삭제 시험용

### 1) 로그인 없이 자료를 달라고 하면

| | 요청 | 결과 |
|---|---|---|
| 성공 | #10 토큰 넣고 `list_plans` | **200** (A의 목록) |
| 거절 | #11 토큰 없이 `list_plans` | **401** `permission denied for function list_plans` |
| 거절 | #12 토큰 없이 `get_plan_bundle` · #13 `export_all` | **401** · **401** |
| 열림(의도) | #14 토큰 없이 `ping` | **200** — 서버가 살아 있는지만 답하는 함수. 자료 없음 |

### 2) 비밀번호

| | 요청 | 결과 |
|---|---|---|
| 성공 | #7 A 이메일 + 맞는 비밀번호 | **200** 토큰 발급 (ES256, 3600초) |
| 거절 | #8 A 이메일 + 틀린 비밀번호 | **400** `invalid_credentials` |
| 거절 | #9 없는 이메일 | **400** `invalid_credentials` — #8과 **상태·문구가 똑같아** 계정이 있는지 알 수 없음 |
| 거절 | #5 A 이메일을 대문자로 바꿔 다시 가입 | **422** (같은 이메일 두 번 가입 안 됨) |
| 거절 | #6 5자 비밀번호로 가입 | **422** (약한 비밀번호) |

A와 B는 같은 비밀번호로 가입했습니다. 비밀번호 원문은 앱 코드·DB 표 어디에도 저장하지 않고, Supabase Auth가 `auth.users.encrypted_password`에 bcrypt로만 둡니다.

### 3) 로그아웃·비밀번호 변경 뒤 같은 토큰

| | 요청 | 결과 |
|---|---|---|
| 성공 | #15 A 토큰으로 `whoami` | **200** |
| — | #16 로그아웃 | **204** |
| 거절 | #17 **같은 주소·같은 헤더·같은 토큰**으로 `whoami` | **401** — 달라진 것은 가운데 로그아웃뿐 |
| 성공 | #22 C 토큰으로 `whoami` → #23 비밀번호 변경 | **200** → **200** |
| 거절 | #24 비밀번호 바꾸기 전에 받아 둔 다른 세션 토큰 | **401** `PDS401` "로그인이 끝났습니다…" |
| 거절 | #26 전체 로그아웃 뒤 남아 있던 토큰 | **401** `PDS401` |
| 거절 | #27 예전 비밀번호로 로그인 | **400** (#28 새 비밀번호는 200) |

토큰 만료 시각(1시간)이 아직 안 지났어도, 서버의 세션 행이 지워지면 `require_user()`가 바로 401로 막습니다.

### 4) 남의 자료 읽기·수정·삭제 (양방향)

A·B에 각각 계획·할 일·실행 기록을 1개씩 넣고(#29~#34), 서로의 번호로 요청했습니다.

| 하려던 일 | 내 자료 (성공) | A → B 자료 | B → A 자료 |
|---|---|---|---|
| 계획 읽기 `get_plan_bundle` | #37 · #44 **200** | #38 **404** | #45 **404** |
| 할 일 고치기 `update_task` | #39 · #46 **200** | #40 **404** | #47 **404** |
| 할 일 지우기 `delete_task` | — | #41 **404** | #48 **404** |
| 실행 기록 지우기 `delete_run_log` | — | #42 **404** | #49 **404** |
| 남의 계획에 할 일 끼워넣기 `create_task` | — | #43 **404** | #50 **404** |

- **없는 번호와 같은 답**: 남의 계획 #4(#38)와 아예 없는 계획 #999999999(#51)가 둘 다 `PDS404` "계획 #…을(를) 찾을 수 없습니다." — 남의 자료가 있다는 사실도 드러나지 않습니다.
- **정말 안 바뀌었나**: 거절 앞(#35·#36)과 뒤(#52·#53)의 `export_all` 건수가 A·B 모두 계획 1·할 일 1·실행 기록 1로 같고, 상대가 고치거나 지운 흔적이 없습니다.

### 5) 주인 바꿔치기·목록

| | 요청 (A 토큰) | 결과 |
|---|---|---|
| 무시됨 | #55 주소에 `?p_owner_id=<B의 ID>` | **200** A 계획 [#3]만 |
| 무시됨 | #56 헤더에 `X-Owner-Id`·`X-User-Id` = B | **200** A 계획 [#3]만 |
| 무시됨 | #57 본문에 `p_owner_id` = B | **200** A 계획 [#3]만 |
| 거절 | #58 토큰 안의 사용자를 B로 고쳐 보냄(서명 불일치) | **401** `PGRST301` |
| 성공 | #59 B의 목록 · #60 A의 내보내기 | **200** — 상대 자료 0건 |

누구인지는 **서명된 토큰의 `sub`에서만** 읽기 때문에, 주소·헤더·본문에 다른 사람 ID를 적어도 바뀌지 않습니다.

### 6) 계정 삭제

| | 요청 | 결과 |
|---|---|---|
| 거절 | #62 확인 문구가 틀린 `delete_my_account` | **400** (안 지워짐) |
| 성공 | #63 확인 문구 "계정 삭제" | **200** |
| 거절 | #64 지운 계정의 토큰으로 `whoami` · #65 그 계정으로 로그인 | **401** · **400** |

## ⑤ AI와 나

**AI에게 맡긴 일**
Claude에게 인증을 붙이는 코드(`app.js`의 가입·로그인·로그아웃·토큰 갱신, `supabase/schema.sql`의 `require_user()`·`owner_id`·404 처리), 로컬 검사, 실제 서버 확인 페이지(`auth-evidence.html`), 이 설명서 ①~④의 초안을 맡겼습니다.

**내가 직접 판단한 일**
인증을 T06과 같은 Supabase 안의 Supabase Auth로 붙이고 같은 저장소에 이어 올리기로 정했습니다. Supabase 설정(Confirm email 끄기, SQL 실행)과 T06 자료를 내 계정으로 옮기는 일은 제가 직접 했습니다. 확인 페이지는 제가 실제 서버에서 돌려 16/16 결과를 봤습니다. 5일 관찰 지표(그날 끝낸 할 일 수)와 바꿀 계획 규칙(할 날·시간대 같이 정하기)도 T06 돌아보기에서 제가 골랐습니다.

**AI 제안을 따르지 않은 일**
AI는 확인이 끝난 시험 계정 A·B를 정리하자고 할 수 있었지만, 승인 전 채점에서 다시 확인할 수 있게 남겨 두기로 했습니다.

## ⑥ 아직 못 막은 것

| 못 막은 것 | 왜 위험한가 |
|---|---|
| **무차별 대입(비밀번호 계속 넣어 보기)** — Supabase 기본 제한(같은 IP에서 로그인·가입 5분에 30번)만 있고, 계정별 잠금·CAPTCHA가 없습니다. | IP를 바꿔 가며 시도하면 약한 비밀번호는 결국 맞힐 수 있습니다. |
| **가입 화면으로 계정이 있는지 알 수 있음** — 로그인 문구는 하나로 맞췄지만, 가입할 때 "이미 가입된 이메일"이 나옵니다(이메일 확인을 꺼서). | 누군가 이 다이어리를 쓰는지 이메일만으로 알아낼 수 있고, 그 이메일을 노린 대입 공격으로 이어집니다. |
| **이메일 주인 확인 없음** — Confirm email을 꺼서 남의 이메일로도 가입됩니다. 비밀번호 재설정도 없습니다. | 남의 이메일을 먼저 선점할 수 있고, 비밀번호를 잊으면 스스로 되찾을 방법이 없습니다. |
| **토큰을 브라우저 탭(sessionStorage)에 둠** — CSP로 외부 스크립트를 막았지만, 같은 출처에서 스크립트가 실행되는 틈(XSS)이 생기면 읽힐 수 있습니다. | 토큰을 가져가면 만료(최대 1시간)나 로그아웃 전까지 그 사람으로 요청할 수 있습니다. |
| **ID가 전체에서 이어지는 번호** — 내가 새로 만든 계획 번호로 전체 자료 수를 대략 짐작할 수 있습니다(남의 내용은 404). | 이용 규모 같은 정보가 조금 샙니다. |
| **두 번째 인증 수단(MFA)·로그인 기록 알림 없음** | 비밀번호가 새면 막을 두 번째 벽이 없고, 모르는 로그인이 있어도 알아채기 어렵습니다. |
