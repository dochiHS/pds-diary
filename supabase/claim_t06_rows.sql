-- =============================================================================
-- T06 자료를 내 계정으로 옮기기 (한 번만 실행)
--
-- 언제: supabase/schema.sql을 실행하고, 앱 첫 화면에서 내 계정으로 가입한 "뒤"에.
-- 어떻게: Supabase 대시보드 → SQL Editor → New query → 아래 한 줄의 이메일을
--         방금 가입한 내 이메일로 바꾼 뒤 Run.
--         (이메일은 이 파일에 저장하지 말고 SQL Editor에서만 바꿔 넣으세요. 저장소는 공개입니다.)
--
-- 하는 일: 주인이 비어 있는 T06 행(계획·수정 이력·할 일·완료 기록·실행 기록·돌아보기)을
--          그 이메일로 가입한 계정으로 옮기고, 빈 주인이 하나도 남지 않으면 주인 칸을 NOT NULL로 잠급니다.
--          값(제목·날짜·시각·ID)은 바꾸지 않습니다. 다시 실행해도 옮길 행이 없으면 아무것도 바뀌지 않습니다.
--
-- 결과 예: {"plans": 2, "tasks": 6, "run_logs": 6, "reviews": 1, "plan_revisions": 1,
--           "task_completions": 6, "plans_without_owner": 0, ...}
-- =============================================================================

select pds_private.claim_t06_rows('여기에-내-이메일@example.com');
